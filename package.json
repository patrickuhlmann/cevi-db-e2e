{
  "name": "hitobito-e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "codegen": "playwright codegen https://cevi.puzzle.ch"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "@types/node": "^26.1.0"
  },
  "dependencies": {
    "otpauth": "^9.5.1"
  }
}
